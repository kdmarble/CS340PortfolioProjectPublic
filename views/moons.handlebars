<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
  <title>Stars</title>
</head>
<body>

  <div class="container">
  <h1>Add a New Moon</h1>
  <form id="addStar" action='/moons' method="post">
    <div class="form-group">
      <label for="inputName">Name</label>
      <input required type="text" class="form-control" id="inputName" name="name" aria-describedby="nameHelp" placeholder="Enter Name">
    </div>

    <div class="form-group">
      <label for="inputPosition">Position</label>
      <input required type="number" class="form-control" id="inputPosition" name="position" aria-describedby="nameHelp" placeholder="Enter Position" min="0" max="3">
    </div>

    <div class="form-group">
      <label for="inputparentplanet">Select Planet you want to add the Moon to</label>
      <select multiple class="form-control" name="planetID" id="inputparentplanet">
        {{#each planets}}
          <option value={{planetID}}>{{name}}</option>
        {{/each}}
      </select>
    </div>

    <button type="submit" class="btn btn-primary">Submit</button>
  </form>
</div>

  <div class="container">
    <h1>Search for a Moon by name</h1>
    <form id ="searchForm" class="searchForm" action="/moons/moon" method="POST">
      <div class="form-group">
          <label for="inputName">Name</label>
          <input required type="text" class="form-control" id="inputName" aria-describedby="nameHelp" placeholder="Enter name" name="name">
        </div>
          <button type="submit" class="btn btn-primary">Submit</button>
    </form>
  </div>

  <div class="container">
    <h2>"Select" for Moons table</h2>
    <table>
      <thead>
        <th>Name</th>
        <th>Position</th>
        <th>PlanetID</th>
      </thead>
      <tbody>
        {{#each moons}}
        <tr>
          <td>{{name}}</td>
          <td>{{position}}</td>
          <td>{{planetID}}</td>
          <td><button onclick="deleteMoon({{moonID}})">Delete</td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>

  <script src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.5.1.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js" integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q" crossorigin="anonymous"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
</body>
</html>
